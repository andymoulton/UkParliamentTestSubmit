export interface PersonViewModel {
  id: number;
  firstName: string;
  lastName: string;
  email: string;
  dateOfBirth: Date;
  departmentId: number;
  departmentName: string;
  managerId: number;
}
